#include <iostream>
using namespace std;
string findSolution(auto target){
string find(auto target,auto history){
if(current == target){
	return history;
}else if(current > target){
	return null;
}else {
	return find(current + 5, "("+history+"+5")
		|| find(current + 3, "("+history+"+3");
}
}
return find(1,"1");
}
int main(int argc, char const *argv[])
{
	   
cout<< findSolution(24);
	return 0;
}